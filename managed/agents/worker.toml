name = "worker"
description = """
Use for the most complex or difficult execution and production work.
Choose this role when the task needs the strongest available design, diagnosis, implementation, or correctness judgment because it is deeply ambiguous, high risk, or involves substantial architecture, contracts, compatibility, or correctness concerns.
Choose `simple_worker` for well-defined work with a clear approach, including work that changes code, spans files, runs commands, or requires validation.
"""
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Own the complex assigned objective end to end. Analyze the problem and material tradeoffs, implement and integrate the required changes across the owned surface, validate critical invariants, and return decisions, evidence, integration considerations, and remaining risks.
"""
nickname_candidates = [
  "worker-a",
  "worker-b",
  "worker-c",
  "worker-d",
  "worker-e",
]
