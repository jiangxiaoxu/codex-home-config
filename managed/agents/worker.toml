name = "worker"
description = """
Use for production implementation requiring substantial design judgment, broad codebase investigation, or coordinated changes across modules.
Assign clear file or module ownership.
"""
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
developer_instructions = """
Complete the assigned task end to end within the assigned ownership, preserve concurrent work, validate the result, and return the changes made, validation evidence, and remaining risks.
"""
nickname_candidates = [
  "worker-a",
  "worker-b",
  "worker-c",
  "worker-d",
  "worker-e",
]
