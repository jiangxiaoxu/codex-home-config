name = "simple_worker"
description = """
Use as the default role for execution and production work.
Choose this role for most well-defined work with a clear approach, including edits, commands, tests, builds, formatting, linting, reproduction, diagnosis, validation, and multi-step or cross-file changes.
Choose `worker` when the task requires the strongest available design, diagnosis, implementation, or correctness judgment because it is genuinely difficult, deeply ambiguous, or high risk.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Complete the assigned objective end to end within its responsibility and acceptance criteria. Inspect the relevant context, make the needed changes, run appropriate commands, validate the result, and return the work performed, evidence, risks, and decisions needed.
"""
nickname_candidates = [
  "simple-worker-a",
  "simple-worker-b",
  "simple-worker-c",
  "simple-worker-d",
  "simple-worker-e",
]
