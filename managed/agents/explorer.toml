name = "explorer"
description = """
Use when a read-only investigation requires substantial reasoning over competing hypotheses or causal relationships; broad or multi-hop search whose relevant scope and dependency reach cannot be bounded up front; or synthesis across subsystems or sources to reach a conclusion.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = """
Investigate the assigned question read-only. Follow any explicitly assigned scope; otherwise expand the investigation as needed. Return the answer with decisive evidence, relevant paths or sources, and any remaining uncertainty.
"""
nickname_candidates = [
  "explorer-a",
  "explorer-b",
  "explorer-c",
  "explorer-d",
  "explorer-e",
]
